<div class="notification-user">
  <p><%= link_to "#{notification.notified_by.name}さんがあなたの投稿に#{notification.notified_type}をしました", link_through_path(notification) %></p>
  <small><%= time_ago_in_words(notification.created_at) %>前</small>
</div>
