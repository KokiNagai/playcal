function headerListOver(number) {
  // document.getElementById('header-lists-' + number).style.borderBottom = "3px solid #9cd75b";
  document.getElementById('header-lists-' + number).style.background = "#f7f7f7";
}

function headerListOut(number) {
  // document.getElementById('header-lists-' + number).style.borderBottom = "";
  document.getElementById('header-lists-' + number).style.background = "";
}
